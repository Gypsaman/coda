Yes, I'd add something — this is direct empirical evidence for a claim coda.md already makes in "Tool-calling and structured output schema changes" ("Each provider implements tool calling with different conventions") and in "Implementation architecture," but right now that claim is asserted, not demonstrated. A short subsection (or an expanded paragraph in "Implementation architecture") walking through 2-3 of the sharpest divergences — especially Anthropic having no native structured-output mode at all (forcing the tool-call emulation) and the three different tool-choice enforcement shapes — would turn an abstract taxonomy claim into a concrete illustration, and ties in naturally with the two-tier prompt-design principle already discussed (why originals stay prose-only, why optimized artifacts lean on these provider-native mechanisms).

I'd keep it tight — a table (provider × {system-prompt mechanism, tool-choice mechanism, structured-output support}) plus a paragraph would do it, not a long new section.